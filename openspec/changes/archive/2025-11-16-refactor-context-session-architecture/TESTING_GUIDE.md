# Phase 10 测试指南

**日期**: 2025-11-09  
**目标**: 验证 Signal-Pull SSE 架构的正确性  
**预计时间**: 2-3 小时

---

## 📋 测试前准备

### 1. 启用功能标志

**文件**: `src/hooks/useChatManager.ts`  
**位置**: 第 20 行左右

```typescript
// 修改前
const USE_SIGNAL_PULL_SSE = false;

// 修改后
const USE_SIGNAL_PULL_SSE = true; // ✅ 启用新架构
```

### 2. 启动后端服务

```bash
# 在项目根目录
cargo run --bin web_service

# 确认后端启动成功，应该看到：
# [INFO] Server running on http://127.0.0.1:8080
```

### 3. 启动前端应用

```bash
# 在项目根目录
npm run tauri dev

# 或者
pnpm tauri dev
```

### 4. 打开浏览器开发者工具

- **Chrome/Edge**: F12 或 Cmd+Option+I (Mac)
- 切换到 **Console** 标签页（查看日志）
- 切换到 **Network** 标签页（查看网络请求）

---

## 🎯 核心测试流程（必做）

### Test 1: 基本消息发送和接收 ⭐⭐⭐

**目标**: 验证最基本的消息流程

**步骤**:
1. 打开应用
2. 创建新聊天（点击 "New Chat"）
3. 在输入框输入: `你好，请介绍一下你自己`
4. 点击发送或按 Enter

**验证点**:

✅ **用户消息立即显示**
- 用户消息应该立即出现在聊天界面
- 内容正确，格式正常

✅ **助手消息占位符出现**
- 应该看到一个空的助手消息框
- 可能有加载动画或光标闪烁

✅ **内容逐字流式显示**
- 助手回复应该一个字一个字地出现
- 不是一次性全部显示

✅ **Console 日志正确**
打开 Console，应该看到以下日志：
```
[ChatManager] Using Signal-Pull SSE architecture (NEW)
[ChatManager] Starting Signal-Pull SSE for chat xxx
[ChatManager] SSE event received: content_delta
[ChatManager] SSE event received: content_delta
...
[ChatManager] SSE event received: message_completed
[ChatManager] Message completed, fetching final state
[ChatManager] Final messages synced: X messages
```

✅ **Network 请求正确**
切换到 Network 标签页，应该看到：
- `POST /api/v1/contexts/{id}/messages` - 发送消息
- `GET /api/v1/contexts/{id}/stream` - SSE 连接（状态: pending）
- 多个 `GET /api/v1/contexts/{id}/messages/{msg_id}/content?from_sequence=N` - 内容拉取

✅ **无错误**
- Console 中没有红色错误
- 消息完整显示，没有截断

**如果失败**:
- 检查后端是否正常运行
- 检查功能标志是否启用
- 查看 Console 错误信息

---

### Test 2: SSE 连接验证 ⭐⭐⭐

**目标**: 验证 EventSource 连接正确建立

**步骤**:
1. 打开 DevTools → Network 标签页
2. 在过滤器中输入 `stream`（只显示 SSE 连接）
3. 发送一条消息
4. 观察 Network 面板

**验证点**:

✅ **SSE 连接建立**
- 应该看到 `GET /api/v1/contexts/{context_id}/stream`
- Type: `eventsource`
- Status: `pending` 或 `200`（长连接）

✅ **事件接收**
- 点击该请求，切换到 **EventStream** 标签页
- 应该看到多个事件：
  ```
  event: content_delta
  data: {"type":"content_delta","context_id":"...","message_id":"...","current_sequence":1,"timestamp":"..."}
  
  event: content_delta
  data: {"type":"content_delta","context_id":"...","message_id":"...","current_sequence":2,"timestamp":"..."}
  
  event: message_completed
  data: {"type":"message_completed","context_id":"...","message_id":"...","final_sequence":10,"timestamp":"..."}
  ```

✅ **Heartbeat 事件**
- 如果等待足够长时间（15秒），应该看到 `heartbeat` 事件
- 这证明连接保持活跃

**如果失败**:
- 检查后端 SSE 端点是否正常
- 检查 CORS 配置
- 查看后端日志

---

### Test 3: 内容增量拉取 ⭐⭐⭐

**目标**: 验证内容是增量拉取的，不是重复拉取

**步骤**:
1. 打开 DevTools → Network 标签页
2. 在过滤器中输入 `content`
3. 发送一条消息
4. 观察内容拉取请求

**验证点**:

✅ **序列号递增**
应该看到多个请求：
```
GET /api/v1/contexts/{id}/messages/{msg_id}/content?from_sequence=0
GET /api/v1/contexts/{id}/messages/{msg_id}/content?from_sequence=1
GET /api/v1/contexts/{id}/messages/{msg_id}/content?from_sequence=2
...
```

✅ **Console 日志显示序列号**
```
[ChatManager] SSE event received: content_delta
[ChatManager] Pulling content from sequence 0
[ChatManager] Content received: sequence=1, length=50
[ChatManager] Pulling content from sequence 1
[ChatManager] Content received: sequence=2, length=50
```

✅ **内容不重复**
- UI 中的文本应该逐渐增长
- 不应该看到重复的内容
- 不应该看到内容闪烁或重置

**如果失败**:
- 检查 `currentSequenceRef` 是否正确更新
- 检查后端返回的 sequence 是否正确

---

### Test 4: 多条消息测试 ⭐⭐

**目标**: 验证连续发送多条消息不会冲突

**步骤**:
1. 发送消息 1: `1+1等于几？`
2. 等待回复完成
3. 发送消息 2: `2+2等于几？`
4. 等待回复完成
5. 发送消息 3: `3+3等于几？`

**验证点**:

✅ **所有消息都成功**
- 3 条用户消息都显示
- 3 条助手回复都完整

✅ **消息顺序正确**
- 消息按发送顺序显示
- 没有乱序

✅ **序列号独立**
- 每条消息的序列号从 0 开始
- 不同消息的序列号不冲突

✅ **SSE 连接复用或重建**
- 检查 Network 面板
- 可能是同一个 SSE 连接，也可能是每次新建
- 两种方式都可以，只要工作正常

**如果失败**:
- 检查 `currentMessageIdRef` 是否正确更新
- 检查是否有序列号冲突

---

### Test 5: 聊天切换和清理 ⭐⭐

**目标**: 验证切换聊天时 SSE 连接正确清理

**步骤**:
1. 创建聊天 A，发送消息
2. 在消息流式传输过程中，创建聊天 B
3. 立即切换到聊天 B
4. 在聊天 B 中发送消息
5. 切换回聊天 A

**验证点**:

✅ **清理日志**
Console 应该显示：
```
[ChatManager] Cleaning up SSE subscription
```

✅ **旧连接关闭**
- Network 面板中，旧的 SSE 连接应该关闭
- 状态变为 `canceled` 或 `finished`

✅ **新连接建立**
- 聊天 B 应该建立新的 SSE 连接
- 使用正确的 context_id

✅ **无消息混淆**
- 聊天 A 的消息不会出现在聊天 B
- 聊天 B 的消息不会出现在聊天 A

✅ **无内存泄漏**
- 多次切换后，应用仍然流畅
- 没有明显的性能下降

**如果失败**:
- 检查 `useEffect` cleanup 是否正确执行
- 检查 `sseUnsubscribeRef` 是否正确调用

---

### Test 6: 错误处理 ⭐⭐

**目标**: 验证网络错误时的优雅降级

**步骤**:
1. 发送一条消息
2. 在流式传输过程中，停止后端服务（Ctrl+C）
3. 观察前端行为

**验证点**:

✅ **错误日志**
Console 应该显示：
```
[ChatManager] SSE error: ...
```

✅ **用户提示**
- 应该看到错误提示: "Connection error. Please try again."
- 或类似的用户友好消息

✅ **连接清理**
```
[ChatManager] Cleaning up SSE subscription
```

✅ **UI 保持响应**
- 应用不会崩溃
- 可以继续操作其他功能

✅ **可以重试**
- 重启后端服务
- 发送新消息应该能恢复正常

**如果失败**:
- 检查错误处理逻辑
- 检查是否有未捕获的异常

---

## 🔍 高级测试（可选）

### Test 7: 长消息流式传输

**步骤**: 发送 `请写一篇500字的文章介绍人工智能`

**验证点**:
- ✅ 内容流畅显示，无卡顿
- ✅ 序列号正确递增（可能到 20-50）
- ✅ 最终内容完整

---

### Test 8: 并发聊天

**步骤**: 
1. 打开聊天 A，发送消息（不等待完成）
2. 立即切换到聊天 B，发送消息
3. 快速在两个聊天间切换

**验证点**:
- ✅ 两个聊天独立工作
- ✅ 没有消息混淆
- ✅ SSE 连接正确管理

---

### Test 9: 内存泄漏检查

**步骤**:
1. 打开 DevTools → Memory 标签页
2. 点击 "Take snapshot"（拍摄快照）
3. 发送 20 条消息
4. 再次点击 "Take snapshot"
5. 比较两个快照

**验证点**:
- ✅ 内存增长 < 50MB
- ✅ 没有大量未释放的对象
- ✅ EventSource 连接正确关闭

---

## 📊 测试结果记录

### 测试结果模板

```markdown
## 测试结果 - 2025-11-09

### Test 1: 基本消息发送和接收
- **状态**: ✅ 通过 / ❌ 失败 / ⚠️ 部分通过
- **测试人**: [你的名字]
- **观察**:
  - 用户消息显示: ✅
  - 流式传输: ✅
  - Console 日志: ✅
  - Network 请求: ✅
- **问题**: [如果有问题，在这里描述]

### Test 2: SSE 连接验证
- **状态**: ✅ / ❌ / ⚠️
- **观察**: ...

[继续其他测试...]
```

---

## ✅ 成功标准

**Phase 10 可以启用的条件**:

1. ✅ Test 1-6（核心测试）全部通过
2. ✅ 没有 Console 错误
3. ✅ 用户体验流畅
4. ✅ 没有明显的性能问题
5. ✅ 错误处理正确

**如果满足以上条件，可以：**
- 保持 `USE_SIGNAL_PULL_SSE = true`
- 进行更广泛的测试
- 准备生产部署

**如果有问题，需要：**
- 记录所有问题
- 修复 bug
- 重新测试
- 直到所有测试通过

---

## 🐛 常见问题排查

### 问题 1: SSE 连接失败

**症状**: Network 中看不到 SSE 连接

**排查**:
1. 检查后端是否运行: `curl http://localhost:8080/health`
2. 检查功能标志是否启用
3. 检查 Console 错误信息

---

### 问题 2: 内容不显示

**症状**: 消息发送了，但没有回复

**排查**:
1. 检查 Console 是否有 `content_delta` 事件
2. 检查 Network 是否有内容拉取请求
3. 检查后端日志是否有错误

---

### 问题 3: 内容重复

**症状**: 同样的内容显示多次

**排查**:
1. 检查序列号是否正确递增
2. 检查 `currentSequenceRef` 是否正确更新
3. 检查是否有多个 SSE 连接

---

### 问题 4: 内存泄漏

**症状**: 应用越来越慢

**排查**:
1. 检查 SSE 连接是否正确关闭
2. 检查 `sseUnsubscribeRef` 是否调用
3. 使用 Memory Profiler 查找泄漏源

---

## 🚀 测试完成后

### 如果所有测试通过

1. **保持功能标志启用**
   ```typescript
   const USE_SIGNAL_PULL_SSE = true; // 保持 true
   ```

2. **进行更广泛的测试**
   - 邀请团队成员测试
   - 测试更多边界情况
   - 收集反馈

3. **准备生产部署**
   - 更新文档
   - 准备回滚计划
   - 监控指标

### 如果有测试失败

1. **记录所有问题**
   - 使用测试结果模板
   - 截图和日志

2. **修复 bug**
   - 优先修复关键问题
   - 逐个验证修复

3. **重新测试**
   - 从失败的测试开始
   - 确保所有测试通过

---

## 📞 需要帮助？

如果遇到问题，可以：

1. **查看文档**:
   - `FRONTEND_MIGRATION_PLAN.md` - 实现细节
   - `FRONTEND_QUICK_REFERENCE.md` - 快速参考
   - `DEPRECATED.md` - 过时代码说明

2. **查看日志**:
   - Console 日志
   - 后端日志
   - Network 请求

3. **联系开发者**:
   - 提供详细的错误信息
   - 提供复现步骤
   - 提供 Console 和 Network 截图

---

**准备好开始测试了吗？从 Test 1 开始！🚀**

