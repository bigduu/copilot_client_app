/**
 * Re-export useChatManager from the new modular structure
 * 
 * This file maintains backward compatibility by re-exporting
 * the hook from its new location in the useChatManager/ directory.
 */

export { useChatManager } from "./useChatManager/index";
