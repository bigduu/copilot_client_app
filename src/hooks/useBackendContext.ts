// Re-export the useBackendContext hook from BackendContextProvider
// This maintains backward compatibility with existing code
export { useBackendContext } from "../contexts/BackendContextProvider";
